import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PROJETO_TRIBO_GRUPO08',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
